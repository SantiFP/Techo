import React from 'react'
import { Typography } from '@mui/material'

export const Home = () => {
  return <Typography variant='h1' textAlign='center' sx={{ textTransform: 'uppercase' }}>Inicio</Typography>
}
